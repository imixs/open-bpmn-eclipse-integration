{
    "private":"true",
    "name":"root",
    "license": "(EPL-2.0 OR GPL-2.0 WITH Classpath-exception-2.0)",
    "scripts": {
        "prepare":"yarn build",
        "build":"yarn build:client && yarn build:server",
        "build:client":"yarn --cwd client install",
        "build:server":"cd server && mvn clean verify"
    }
}